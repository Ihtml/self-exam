### 重点1 范式

1. 关系模式中肯存在的冗余和异常问题
   - 数据冗余：同一数据反复存储的情况
   - 更新异常：数据冗余将导致潜在数据不一致的情况
   - 插入异常：应该插入到数据库中的数据不能执行插入操作的情形
   - 删除异常：不应该删去的数据被删去



2. 函数依赖
   - 定义：设R(U)是一个属性集U上的关系模式（二维表），X和Y是U的子集。若对于R(U)的任意一个可能的关系r, r中不可能存在两个元组在X上的属性值相等，而在Y上的属性值不等，则称“X函数确定Y” 或 “Y函数依赖于X”，记作X->Y
   - 分类：
     - 完全函数依赖：在关系模式R(U)中，如果X->Y, 并且对于X的任何一个真子集X‘ ， 都有 X' - /->Y 则称Y完全函数依赖于X
     - 部分函数依赖：若X->Y, 但Y不完全函数依赖于X， 则称Y部分函数依赖于X
     - 传递函数依赖：在关系模式R(U)中，如果X->Y, Y->Z, 且Y-/->X, 则有X->Z, 称Z传递函数依赖于X。



3. 范式

   - 定义：关系要满足一定的要求，满足不同程度的要求称为不同的范式
- 第一范式：关系模式R的**所有属性都是不可分的基本数据项**。第一范式是对关系模式的最起码要求。不满足第一范式的数据库模式不能称为关系数据库。
   - 第二范式：若关系模式R是第一范式，且**每一个非主属性都完全函数依赖于R的码**，则R属于第二范式。(一般地，所有的单关键字都符合第二范式)
- 第三范式：关系模式R属于第二范式，且每一个非主属性都不传递函数依赖于候选关键字，则R为第三范式。
   - 总结：
- 一范式到二范式：消去非主属性对键的部分函数依赖
     - 二范式到三范式：消去非主属性对键的传递函数依赖
  - 三范式到BCNF：消去主属性对键的传递函数依赖
   - 技巧：哪里有问题就分解哪里



### 重点2 ER图与关系模式

1. **概念模型**的表示方法- ER图
   1. 实体型间联系
      - 一对一联系 (1:1)
      - 一对多联系 (1:n)
      - 多对多联系 (m:n)
   2. 画图表示
      - 实体用矩形，属性用椭圆形，联系用菱形
      - **主键**用**下划线**表示，**外键**用**波浪线**表示

2. 逻辑结构设计方法——转为关系模式

   二元联系类型的转换：

   - 若实体间联系是1：1，可以在两个实体类型转换成的两个关系模式中的任何一个关系模式的属性中加入另一个关系模式的键（作为外键）和联系类型的属性
   - 若实体间联系是1：N, 则在N端实体类型转换成的关系模式中加入1端实体类型的键（作为外键）和联系类型的属性
   - 若实体间的联系是M：N，则将联系类型也转换成关系模式，其属性为两段实体类型的键（作为外键）加上联系类型的属性，而键为两段实体键的组合。



### 重点3 关系代数和SQL语句（20'）

1. 关系操作的集合
   - 基本操作：并差积选投
   - 组合操作：交连自除



2. 基本操作
   - 并(U): 两个关系需要有相同的关系模式，并的对象是元组，由两个关系所有元组构成。
   - 差(-): 同样，两个关系有相同的模式，R和S的差是由属于R但不属于S的元组构成的集合。
   - **笛卡尔积(x)**: 对于两个关系运算，**列**：(n+m)列的元组的集合，元组的前n列是关系R的一个元组，后m列是关系S的一个元组，行：k1 * k2个元组
   - **选择($\sigma$)**：根据某些条件关系作碎片分割，即选择**符合条件的元组**。 $\sigma$小区名=‘万科城’（房屋）
   - **投影(π)**: 对关系进行垂直分割，消去某些列，并重新安排列的顺序。π会员名，联系电话(会员)



3. SQL语句

   - 查询语句

     select [all | distinct | distinctrow] 属性1[,属性2， 属性3]

     from 表名

     [where 条件]

     [group by 字段]

     ​	[having 条件]

     [order by 属性 {asc | desc}]

   - 创建视图

     create view 视图名[(列名)]

     as select 语句

   - 修改数据

     Update 表名

     ​	set 列名=值

     ​	[where 条件]

   - 创建表

     create table 表名

     (

     ​	字段名1 数据类型 [列级完整性约束]

     ​	[, 字段名2 数据类型 [列级完整性约束]]

     ​	[, 表级完整性约束]

     )



### 第一章 数据库系统概述

1. 简述数据的概念p27

   数据是描述事物的符号记录，是指用物理符号记录下来的，可以鉴别的信息。

   数据有多种表现形式，可以是文本数据，也可以是多媒体数据。

   

2. 数据库管理系统包括哪些功能p28-29

   数据定义功能、数据操纵功能、数据库的运行管理功能、数据库的建立和维护功能、数据组织、存储和管理功能

   

3. 什么是并发控制p32-33

   并发控制是指当多个用户的并发进程同时存取、修改数据库时，可能会发生相互干扰而得到错误结果，并使得数据库的完整性遭到破坏，因而对多用户的并发操作加以控制和协调。

   

4. 什么是数据模型p38

   - 数据模型是用来描述数据的结构、定义在结构上的操纵以及数据间的约束的一组概念和定义
   - 数据模型通常由：数据结构、数据操作和数据约束组成。

   

5. 简述关系模型的优点p41

   - 关系模型是建立在严格的数学概念的基础上的

   - 关系模型的概念单一，统一用关系来表示实体以及实体之间的联系，对数据的检索和更新结果也是用关系来表示，关系模型的数据结构简单、清晰，用户易懂、易用。

   - 关系模型的存取路径对用户透明，从而具有更高的数据独立性、更好的安全保密性，也简化了程序员的工作和数据库开发建立的工作

     

6. 简述物理数据独立性p36

   数据库系统的物理存储如若发生改变，（例如选用一种存储结构或更换另外一个存储位置）数据库管理员DBA通常也会对模式/内模式映像做出相应的调整，以使数据库系统的模式保持不变，从而也不必去修改应用程序，如此实现了概念模式不受内模式变化的影响，并保证了数据与程序的物理独立性。

   

7. 简述数据独立性的概念p32

   - 数据定义与使用数据的应用程序分离称为数据独立。即数据或应用程序的修改不会彼此引起另一方的修改。数据库系统提供两层数据独立。

   - 数据的物理独立：可改变数据的存储结构或存取方法以响应变化的需求，而无需修改现有的应用程序。

   - 数据的逻辑独立：应用程序在一定范围内修改时，可以只修改它的数据库视图，而不修改数据本身的定义；数据定义的修改，在一定范围内不会引起应用程序的修改。

     

8. 简述客户/服务器结构和浏览器/服务器结构p37

   

9. 什么是三级模式，两级映像，分别有什么作用p35

   - 模式：数据库中全体数据的逻辑结构和特征的描述，是所有用户的公共数据视图。
   - 外模式：是数据库用户能够看见和使用的局部数据的的逻辑结构和特征的描述。
   - 内模式：对数据库中数据物理结构和存储方式的描述，是数据在数据库内部的表示形式。

   - 外模式/模式映像：定义了各个外模式与概念模式之间的映像关系，这些映像关系定义通常在各自的外模式中加以描述。

   - 模式/内模式映像：定义了数据库全局逻辑结构与物理存储之间的对应关系，这种映像定义通常是在模式中加以描述的。（由于数据库中只有一个模式，且也只有一个内模式，所以模式/内模式映像是唯一的。）

     

10. 数据模型分为哪几层p39-41

   - 概念层数据模型
   - 逻辑层数据模型
   - 物理层数据模型



### 第二章 关系数据库

1. 关系数据库的基本特征是什么p43

   使用关系数据模型组织数据

   

2. 简述关系模式中可能存在的冗余和异常问题p56-57

   - 数据冗余：同一数据被反复存储的情况

   - 更新异常：数据冗余导致存储空间的浪费和潜在数据不一致性及修改麻烦等问题

   - 插入异常：应该插入到数据库中的数据不能执行插入操作的情形

   - 删除异常：不应该删去的数据被删去的情形

     

3. 请简述关系规范化过程p56-61

   一个低一级范式的关系模式，通过**模式分解**可以转换为若干个高一级范式的关系模式的集合，这种过程就叫规范化。

   

4. 什么是关系模型的完整性约束检验p55

   为了维护关系数据库中数据的完整性，在对关系数据库执行插入、删除和更新操作时，需要检验是否满足实体完整性约束、参照完整性约束、用户定义完整性约束

   

5. 什么是完全函数依赖p58

   设R为任一给定关系。X、Y为其属性集，若X->Y,且对X中的**任何真子集**X'都有X'-/->Y,则称Y完全函数依赖于X。

   

6. 什么是部分函数依赖p58

   设R为任一给定关系。X、Y为其属性集，若X->Y,且对X中**存在一个真子集**X'都有X'->Y,则称Y部分函数依赖于X。

   

7. 什么是范式/第一范式/第二范式/第三范式p59-63

   - 范式：关系数据库中的关系需要满足一定的要求，不同程度的要求称为不同的范式。满足最低要求的称为第一范式；在第一范式的基础上进一步满足一些新要求的称为第二范式，再进一步是第三范式及其改进形式BCNF。
   - 第一范式：是一个不含重复组的关系，其中不存在嵌套结构。
   - 第二范式：设R为任一给定关系，若R为1NF，且其所有非主属性都完全函数依赖于候选关键字，则R为第二范式。
   - 第三范式：设R为任一给定关系，若R为2NF，且其每一个非主属性都不传递函数依赖于候选关键字，则R为第三范式。
   - 1Nf->2NF: 消除非主属性对码的部分函数依赖
   - 2NF->3NF: 消除非主属性对码的传递函数依赖
   - 3NF->BCNF: 消除主属性对码的部分和传递函数依赖（保证主键的唯一性）

   

8. **(重点)**元组、分量、码、超码、候选码、主码、全码、主属性、域、关系模式的定义p44-46

   - 元组：表中的一行即为一个元组。
   - 分量：元组中的一个属性值。
   - 码(键)：如果在一个关系中，存在这样的属性（或属性组），使得在该关系的任何一个关系状态中的两个元组，在该属性（或属性组）上值的组合都不同，即这些属性（或属性组）的值都能用来唯一标识该关系的元组，则这些属性（或属性组）为该关系的码或键。
   - 超码（超键）：如果在关系的一个码中移去某个属性，它仍然是这个关系的码，则称这样的码或键为该关系的超码或超键。
   - 候选码：如果在关系的一个码或键中，不能从中移去任何一个属性，否则它就不是这个关系的码，则称这样的码为该关系的候选码或候选键。一个关系的候选码是这个关系的最小超码。
   - 主码(主键)：在一个关系的若干个候选码或候选键中**指定一个用来唯一标识**关系的元组，则称这个被指定的候选码为该关系的主码。
   - 全码：一个关系模式的所有属性集合是这个关系的主码或主键，则称这样的主码或主键为全码或全键。
   - 主属性：包含在任何一个候选码中的属性称为主属性或码属性，不包含在任何一个候选码中的属性称为非主属性或非码属性。
   - 域：属性的取值范围。
   - 关系模式：同数据模型一样，数据库也有型(type)和值(value)之分。在关系数据库中，关系模式是型，关系是值，即关系模式是对关系的描述。关系模式是静态、稳定的，而关系是动态的、随时间不断变化的。



### 第三章 数据库设计

1. 简述数据库设计的方法的分类p65-66

   - 直观设计法

   - 规范设计法

   - 计算机辅助设计法

     

2. 简述需求分析的步骤p67-70

   - 确定数据库范围

   - 分析数据应用过程

   - 收集与分析数据

   - 编写需求分析报告

     

3. 什么是数据字典p70

   - 是数据库系统中存储三级结构定义的数据库，通常指的是数据库系统中数据各类详细描述的集合。

   - 功能是存储和检索各种数据描述，即元数据。

   - 提供了各类数据描述的接种管理，是一种数据分析、系统设计和管理的有力工具。

     

4. 数据库实施阶段需要完成哪些工作p72-73

   - 加载数据

   - 应用程序设计

   - 数据库试运行

     

5. 数据库的生命周期p64

   - 数据库分析与设计阶段：需求分析、概念设计、逻辑设计、物理设计
   - 数据库实现与操作阶段：数据库的实现、操作与监督、修改与调整



### 第四章 SQL与关系数据库基本操作

1. 简述SQL的特点p85

   - SQL不是某个特定数据库供应商专有的语言

   - SQL简单易学

   - SQL尽管看上去很简单，但它实际上是一种强有力的语言，灵活使用其语言元素，可以进行非常复杂和高级的数据库操作。

     

2. 简述SQL的组成部分p85-86

   - 数据定义语言 DDL （create alter drop)

   - 数据操作语言 DML (select insert update delete)

   - 数据控制语言 DCL （grant revoke ）

   - 嵌入式和动态SQL规则

   - SQL调用和回话规则：包括SQL例程和调用规则，以便提高SQL的灵活性、有效性、共享性以及使SQL具有更多的高级语言的特性。

     

3. 数据操纵语言包括哪些SQL语句p85-86

   - select：用于从表或视图中检索数据

   - insert：用于将数据插入到表或视图中

   - update：用于修改表或视图中的数据，其既可修改表或视图中的一行数据，也可以同时修改多行或全部数据。

   - delete：用于从表或视图中删除数据，其中可根据条件删除指定数据。

     

4. LAMP/WAMP架构方式分别是什么含义p86

   - Linux + Apache + Mysql + PHP/Perl/Python

   - Windows + Apache + Mysql + PHP/Perl/Python

     

5. MySQL中函数主要有哪些p88

   - 数学函数：abs() sort()

   - 聚合函数：count()

   - 字符串函数: ascii()  char()

   - 日期和时间函数: now() year()

   - 加密函数: encode() encrypt()

   - 控制流程函数: if() ifnull()

   - 格式化函数: format()

   - 类型转换函数: cast()

   - 系统信息函数: user() version()

     

6. **简述视图表和表的区别。视图的优点（重点）**p119

   概念：外模式对应到数据库中的概念就是视图

   区别：

   - 视图不是数据库中的真实的表，而是一张虚拟表，其结构和数据是建立在对数据库中真实表的查询基础上的。
   - 视图的内容是由存储在数据库中进行查询操作的SQL语句来定义的，它的列数据和行数据均来自于定义视图的查询所引用的真实表，并且这些数据是在引用视图时动态生成的。
   - 视图不是以数据集的形式存储在数据库中，它所对应的数据实际上是存储在视图所引用的真实表（基本表）中。
   - 视图是用来查看存储在别处的数据的一种虚拟表，而其自身不存储数据。

   优点：

   - 集中分散数据。当用户所需的数据分散在数据库多个表中时，通过定义视图可以将这些分散的数据集中在一起，以便用户对分散数据的集中查询与处理。

   - 简化查询语句。

   - 重用SQL语句。

   - 保护数据安全。

   - 更改数据格式。

     

7. SQL各版本的年份p84

   - 1986.10 美国国家标椎局（American National Standard Institute）ANSI 最早标椎
   - 1987.6  被国际标椎化组织（International Organization for Standardization）ISO采纳为国际标椎
   - 1988.4， ISO提出了具有完整性特征的SQL， SQL-89
   - 1992, ISO 和ANSI共同颁布SQL-92 标椎 SQL2
   - 1999， ISO和ANSI颁布SQL-99， SQL3



### 第五章 数据库编程

1. 存储过程的概念及其优点p125-126

   - 存储过程是一组为了完成某项特定功能的SQL语句集，其本质上就是一段存储在数据库中的代码，它可以由**声明式的SQL语句**(create、update、select)和**过程式SQL语句**（if..then...else控制结构语句）组成。
   - 这组语句集经过编译后会存在数据库中，用户只需通过指定存储过程的名字并给定参数，即可随时调用并执行它，而不必重新编译，加大数据库操作语句的执行效率。
   - 一个存储过程是一个可编程的函数，同时可看作是在数据库编程中对面向对象方法的模拟，它允许控制数据的访问方式。

   好处：

   - 可增强SQL语言的功能和灵活性

   - 良好的封装性

   - 高性能

   - 可减少网络流量

   - 存储过程可作为一种安全机制来确保数据库的安全性和数据的完整性。

     

2. 常用的循环语句有哪些p129

   - while repeat loop

   - 循环语句中还可以使用iterate，表示退出当前循环，且重新开始一个循环。

     

3. 声明游标的具体步骤p129-130

   - 声明游标：declare cursor

     `DECLARE cursor_name CURSOR FOR select_statement`

   - 打开游标： open cursor_name

   - 读取数据：fetch ... into

     `FETCH cursor_name INTO var_name`

   - 关闭游标: CLOSE cursor_name

     

4. 局部变量的用法p128

   - 在存储过程体中可以声明局部变量，用来存储过程体中的临时变量。
   - 局部变量只能在存储过程体的BEGIN...END 语句块中声明
   - 局部变量必须在存储过程体的开头处声明
   - 局部变量的作用范围仅限于声明它的BEGIN...END语句块，其他语句块中的语句不可以使用它。
   - 局部变量不同于用户变量，两者的区别是：局部变量声明时，在其前面没有使用@符号，并且它只能被声明它的BEGIN...END语句块所使用。用户变量声明时，会在其前面使用@符号，同时已声明的用户变量存在于整个会话中。

   

5. 存储过程和存储函数的联系和区别p132

   - 存储过程和存储函数一样，都是有SQL语句和过程式语句所组成的代码片段，并且可以被应用程序和其他SQL语句调用。

   区别：

   - 存储函数不能拥有输出参数，这是因为存储函数自身就是输出参数；而存储过程可以拥有输出参数。
   - 可以直接对存储函数进行调用，且不需要使用CALL语句；而对存储过程的调用，需要使用CALL语句。
   - 存储函数中必须包含一条RETURN语句，而这条特殊语句不允许包含于存储过程中。



### 第六章 数据库安全与保护

1. 排它锁（X锁）和共享锁（S锁）是如何协调的 p155

   - 若事务T对数据D加了X锁，则所有别的事务对数据D的锁请求都必须等待，直到事务T释放锁。

   - 若事务T对数据D加了S锁，则别的事务还可以对数据D请求S锁，而对数据D的X锁请求必须等待，直到事务T释放锁。。

   - 事务执行数据库操作时都要先请求相应的锁，即对读请求S锁，对更新请求X锁。这个过程一般是由DBMS在执行操作时自动隐含地进行。

   - 事务一直占有获得的锁直到结束（COMMIT或ROLLBACK）时释放。

     

2. Commit和rollback的功能 p152

   - 事务通常是BEGIN TRANSACTION语句开始，以COMMIT或ROLLBACK语句结束

   - COMMIT语句表示提交，即提交事务的所有操作，具体地说就是将事务中的所有对数据库的更新写回到磁盘上的物理数据库中去，事务正常结束。

   - ROLLBACK语句表示回滚，即在事务运行过程中若发生了某种故障，事务不能继续执行，系统将事务中对数据库的所有已完成的操作全部撤销，回滚到事务开始时的状态。

     

3. 并发操作会带来哪些问题（**每个问题都要有概念**） p154

   - 丢失更新

   - 不可重复读

   - 读“脏”数据

     

4. 简述事务的性质（**每个性质都要懂**）p152-153

   - 原子性（Atomicity）:事务是不可分割的最小工作单位，所包含的操作是一个整体

   - 一致性（Consistency）：事务必须满足数据库的完整性约束，且事务执行完毕后将数据库由一个一致性状态转变到另一个一致性状态。

   - 隔离性（Isolation）：事务彼此独立、隔离，一个事务的执行不能被其他事务所干扰，一个事务对数据库变更的结果必须在它COMMIT后，另一个事务才能存取。

   - 持续性（Durability）：一个事务一旦提交，它对数据库中数据的改变就应该是永久性的，且接下来的其他操作或故障不应该对其执行结果有任何影响。

     

5. 什么是数据库的完整性 p54/135

   - 数据库的数据完整性是指数据库中数据的正确性、相容性和一致性。
   - 数据库完整性是指数据中数据的**正确性**和**相容性**。

   

6. 什么是安全性 p144 、一致性 p153

   - 安全性：保护数据库以防止不合法的使用而造成数据泄露、更改和破坏

   - 数据库的一致性状态：是一种以一致性规则为基础的逻辑属性。

     

7. 授予权限的SQL语句 p150-151

   Grant 

   ​		priv_type [(column_list)]

   ​			[, priv_type [(column_list)]] ...

   ​		ON  [object_type] prive_level

   ​		TO user_specification [, user_specification]...

   ​		[WITH GRANT OPTION]

   

   Grant SELECT, UPDATE

   ​		ON mysql_test.customs

   ​		TO 'lisi'@'localhost' IDENTIFIED BY '123'

   ​		TO 'zhangsan'@'localhost' IDENTIFIED BY '789'

    

8. 数据库故障有哪些 p157-158

   - 计算机硬件故障

   - 计算机软件故障

   - 病毒

   - 人为误操作

   - 自然灾害

   - 盗窃

     

9. 封锁的粒度 p155

   - 通常以粒度来描述封锁的数据单元的大小。

   - DBMS可以决定不同粒度的锁。

   - 由最底层的数据元素到最高层的整个数据库，粒度越细，并发性就越大，但软件复杂性和系统开销也就越大。

     

10. 封锁的级别 p155-156

  - 封锁的级别又称为一致性级别或隔离度。它与封锁的期限有关，依应用的不同要求，封锁的期限可能不一样，由各种锁的类型与其封锁期限组合可形成不同的封锁级别。
  - 0级封锁：封锁的事务不重写其他非0级封锁事务的未提交的更新数据。
  - 1级封锁：被封锁的事务不允许重写未提交的更新操作。防止了丢失更新的发生。
  - 2级封锁：被封锁的事务既不重写也不读未提交的更新数据。这除了1级封锁的效果还防止了读脏数据。
  - 3级封锁：被封锁的事务不读未提交的更新数据，不写任何（包括读操作的）未提交数据。除了包含2级封锁外，还不写未提交的读数据，因而防止了不可重读的问题。这是严格的封锁，它保证了多个事务并发执行的“可串行化”。

  

11. 主键约束的方式有哪些 p136

    - 一个表只能定义一个主键。

    - 主键的值，也称键值，必须能够唯一标志表中的每一行记录，且不能为NULL。

    - 复合主键不能包含不必要的多余列。（最小化规则）

    - 一个列名在复合主键的列表中只能出现一次。

      

12. **常用的数据库安全和保护机制有哪些（重点）** p161

    - 完整性约束（包括触发器）
    - 访问控制
    - 事务与并发控制
    - 备份与恢复



### 第七章 数据库应用设计与开发实例

1. 简述功能性需求和非功能性需求（可靠、正确、兼容、健壮） p162-163

   

2. 简述数据库应用软件设计与实现中数据库设计的过程  p165-170

   1. 确定实体
   2. （建立）局部信息结构
   3. （建立）全局信息结构
   4. 逻辑结构与规范化设计

   根据系统的功能模块设计结果，以及前期的需求分析，可首先明确本系统的数据库范围，然后可通过使用E-R图作为数据库概念设计的描述工具，建立系统所设计的局部信息结构，再讲各个局部信息结构合并成为一个优化的全局信息结构，最后将全局信息结构的E-R图转换为关系模型，并依据关系数据模型规范化理论进行优化。



###  第八章 数据库管理技术的发展

1. 简述数据挖掘的功能 p183-184

   - 概念描述：通过数据挖掘技术，可以归纳总结出数据的某些特征。

   - 关联分析

   - 分类与预测

   - 聚类

   - 孤立点检测

   - 趋势和演变分析

     

2. 简述大数据的特征 p185-186

   - 数据量巨大，及大量化（Volume）

   - 数据种类繁多，及多样化（Variety）

   - 处理速度快，及快速化（Velocity）

   - 价值（Value）密度低。

     

3. NoSQL有哪些类型及典型数据库 p187

   NoSOL是以互联网大数据应用为背景发展起来的分布式数据库管理系统。有两种解释：一种是Non-Relational，即非关系型数据库；另一种是Not Only SQL，即数据管理技术不仅仅是SQL。

   - 键值存储：Redis、OracleBDB、 Tokyo Cabinet、Tyrant

   - 文档存储：MongoDB、CouchDB

   - 列存储：Cassandra、HBase

     

4. 什么是Mapreduce  p187

   - MapReduce 技术是Google 于2004年提出的大规模并行计算解决方案，主要应用于大规模廉价集群上的大数据并行处理。以key/value的分布式系统为基础，通过元数据集中存储、数据以chunk为单位分布存储和数据chunk冗余复制来保证其高可用性。

   - 是一种并行编程模型。把计算过程分解为两个阶段，即Map阶段和Reduce阶段。

   - 是一种简单易行的软件框架。

     

5. 数据仓库的特点 p182

   - 面向主题

   - 集成性

   - 数据的非易失性

   - 数据的时变性

     

6. 第三代数据库的特点 p180

   - 应支持数据管理、对象管理和知识管理
   - 必须保持或继承第二代数据库系统的技术
   - 第三代数据库系统必须对其他系统开放